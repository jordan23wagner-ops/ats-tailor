import{Resend}from"resend";export const config={maxDuration:60};const resend=new Resend(process.env.RESEND_API_KEY);export default async function handler(req,res){res.setHeader("Access-Control-Allow-Origin","*");if(req.method==="OPTIONS")return res.status(200).end();if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});try{let body=req.body;if(typeof body==="string"){const p=new URLSearchParams(body);body=Object.fromEntries(p.entries());}const buyerEmail=body.email||body.buyer_email;const buyerName=body.full_name||body.buyer_full_name||"";let fields={};try{fields=JSON.parse(body.custom_fields||"{}");}catch{}const resume=fields.resume||fields.Resume||"";const jobDescription=fields.job_description||fields["Job Description"]||"";if(!resume||!jobDescription)return res.status(200).json({received:true,error:"Missing fields"});const r=await fetch("https://y-delta-lake.vercel.app/api/analyze",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({resume,jobDescription})});if(!r.ok)throw new Error("Analyze API "+r.status);const result=await r.json();await resend.emails.send({from:"ATS Tailor <results@ats-tailor.org>",to:buyerEmail,subject:"Your ATS Score: "+result.atsBefore+" → "+result.atsAfter,html:"<h2>Ready, "+(buyerName||"there")+".</h2><p>Before: "+result.atsBefore+" | After: "+result.atsAfter+"</p><p>"+result.summary+"</p>"});return res.status(200).json({success:true});}catch(err){return res.status(200).json({received:true,error:err.message});}}
